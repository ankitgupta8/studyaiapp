# RemNote Flashcard Generation - Task List

- [ ] 1. Update `Mode` type in `src/types/index.ts`
- [ ] 2. Add `remnoteContent` field to Prisma schema + push DB
- [ ] 3. Add `generateRemNoteFlashcards` and `generateRemNoteFromChunks` to AI service
- [ ] 4. Update `/api/generate` route for `remnote` mode
- [ ] 5. Update `/api/upload-pdf` route for `remnote` mode
- [ ] 6. Update `page.tsx` — state, mode toggle, generate handlers, results display
- [ ] 7. Verify: build + dev server works
